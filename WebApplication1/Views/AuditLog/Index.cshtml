@model IEnumerable<WebApplication1.Entities.AuditLog>

<h2>Audit Logs</h2>

<table class="table">
    <thead>
        <tr>
            <th>User ID</th>
            <th>Action</th>
            <th>Timestamp</th>
            <th>IP Address</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var log in Model)
        {
            <tr>
                <td>@log.UserId</td>
                <td>@log.Action</td>
                <td>@log.Timestamp</td>
                <td>@log.IpAddress</td>
                <td>@log.Details</td>
            </tr>
        }
    </tbody>
</table>
